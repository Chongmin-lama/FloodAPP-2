The migration-script is the script query to run in the mssql of the user wants to run locally
else

create a env file and 
add this in that file
DB_SERVER=MSSQL_ServerName
DB_DATABASE=floodguard
DB_USER=Server_userName	
DB_PASSWORD=Server_password
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true